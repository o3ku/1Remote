<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type Hyperlink}">
        <Setter Property="Foreground" Value="#297ccf"></Setter>
        <Setter Property="TextBlock.TextDecorations" Value="{x:Null}"></Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource AccentMidBrush}"></Setter>
                <Setter Property="TextBlock.TextDecorations">
                    <Setter.Value>
                        <TextDecorationCollection>
                            <TextDecoration Location="Underline" />
                        </TextDecorationCollection>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>


    <SolidColorBrush x:Key="FilledBrush" Color="#FFFFFFFF"/>


    <!--
    一个转圈圈的 view，这样使用：
    <Control Style="{StaticResource BusyAnimationStyle}" Background="Transparent"/>-->
    <Style x:Key="BusyAnimationStyle" TargetType="Control">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Control">
                    <Border Background="{TemplateBinding Background}">
                        <Grid Width="100">
                            <Canvas Height="60" Width="60" Margin="12 0 0 0">
                                <Canvas.Resources>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Width" Value="8"/>
                                        <Setter Property="Height" Value="8" />
                                        <Setter Property="Fill" Value="{StaticResource FilledBrush}" />
                                        <Setter Property="Opacity" Value="0.35" />
                                    </Style>
                                </Canvas.Resources>
                                <Ellipse Canvas.Left="1.75" Canvas.Top="21"/>
                                <Ellipse Canvas.Top="7" Canvas.Left="6.5"/>
                                <Ellipse Canvas.Left="20.5" Canvas.Top="0.75"/>
                                <Ellipse Canvas.Left="34.75" Canvas.Top="6.75"/>
                                <Ellipse Canvas.Left="40.5" Canvas.Top="20.75" />
                                <Ellipse Canvas.Left="34.75" Canvas.Top="34.5"/>
                                <Ellipse Canvas.Left="20.75" Canvas.Top="39.75"/>
                                <Ellipse Canvas.Top="34.25" Canvas.Left="7" />
                                <Ellipse Width="39.5" Height="39.5" Canvas.Left="8.75" Canvas.Top="8" Visibility="Hidden"/>
                            </Canvas>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
